import { Subcategoriafilter } from './subcategoriafilter';

describe('Subcategoriafilter', () => {
  it('should create an instance', () => {
    expect(new Subcategoriafilter()).toBeTruthy();
  });
});
